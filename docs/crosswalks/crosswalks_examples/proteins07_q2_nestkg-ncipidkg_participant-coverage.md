# P07-Q2: How many NeST proteins appear in NCI-PID — 526 overall, 506 as interaction participants

- **Date:** 2026-09-01
- **Model:** claude-opus-5[1m]
- **SPARQL endpoint:** https://apps.okn.us/federation/sparql
- **Generated by:** mcp-okn v0.1.0 (build 3638bbc)
- **Contents:** 2 queries

## Knowledge graphs used

- `nestkg` — <https://purl.org/okn/frink/kg/nestkg>
- `ncipidkg` — <https://purl.org/okn/frink/kg/ncipidkg>

## Conversation

👤 **User**

Same crosswalk, coverage angle: how many NeST proteins take part in at least one NCI-PID interaction?

---

🧠 **Assistant**

**526 NeST proteins have a record in ncipidkg; 506 of them are participants in a reified NCI-PID interaction statement.**

| Measure | Count |
|---|---|
| ncipidkg distinct UniProt interaction participants (`rdf:subject` ∪ `rdf:object`) | 2,497 |
| … also a NeST protein | 506 (20.3% of ncipidkg) |
| NeST proteins with *any* ncipidkg record (the catalogued crosswalk count) | 526 |

**The 526 / 506 gap is worth understanding, not smoothing over.** 526 is the join measured as "NeST protein appears anywhere in ncipidkg" — the count stored in the crosswalk table. 506 is the stricter "NeST protein is the subject or object of a reified interaction statement". The 20 in between appear in ncipidkg only through its other surfaces: the direct (unreified) `RO_0002436` / `RO_0002578` edges and the `owl:sameAs` leg. If your question is *"which NeST proteins have evidence-weighted signalling?"*, 506 is the honest denominator; 526 is the reachability of the join.

**Read against the other side.** 506 of ncipidkg's 2,497 participants — one in five — is a NeST protein. That is a much higher hit rate than the AOP-Wiki side of the same hub (18.5%, but from a 7,437-protein pool), and it reflects what NCI-PID is: a curated *cancer* signalling resource, so its proteins are drawn from much the same biology NeST reconstructs from data.

**Scale caveat.** 526 is small next to NeST's 16,767 proteins (3.1%). NCI-PID is a curated pathway set, not a proteome, so this join is for depth on well-studied signalling proteins — not for coverage of the NeST map. For breadth across NeST, use the ProKN join (16,729, 99.8%).

## SPARQL queries executed

#### Query 1

_2026-09-02T04:55:51+00:00 · `ncipidkg`, `nestkg`_

```sparql
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX sio: <http://semanticscience.org/resource/>
SELECT (COUNT(DISTINCT ?prot) AS ?ncipidUniProtParticipants) (COUNT(DISTINCT ?inNest) AS ?alsoNestProteins) WHERE {
  GRAPH <https://purl.org/okn/frink/kg/ncipidkg> {
    { ?st rdf:subject ?prot } UNION { ?st2 rdf:object ?prot }
    FILTER(STRSTARTS(STR(?prot),'http://purl.uniprot.org/uniprot/'))
  }
  OPTIONAL {
    GRAPH <https://purl.org/okn/frink/kg/nestkg> { ?prot a sio:SIO_010043 }
    BIND(?prot AS ?inNest)
  }
}
```

_1 row(s)_

| ncipidUniProtParticipants | alsoNestProteins |
| --- | --- |
| 2497 | 506 |

#### Query 2

_2026-09-02T04:56:01+00:00 · `nestkg`, `ncipidkg`_

```sparql
PREFIX sio: <http://semanticscience.org/resource/>
SELECT (COUNT(DISTINCT ?up) AS ?nestProteinsInNcipid) WHERE {
  GRAPH <https://purl.org/okn/frink/kg/nestkg> { ?up a sio:SIO_010043 }
  GRAPH <https://purl.org/okn/frink/kg/ncipidkg> { ?up ?p ?o }
}
```

_1 row(s)_

| nestProteinsInNcipid |
| --- |
| 526 |

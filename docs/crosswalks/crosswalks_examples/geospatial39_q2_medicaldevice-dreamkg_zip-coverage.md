# GEO39-Q2: 50 of DreamKG's 53 service ZIPs also carry a medical-device-kg address

- **Date:** 2026-09-01
- **Model:** claude-opus-5[1m]
- **SPARQL endpoint:** https://apps.okn.us/federation/sparql
- **Generated by:** mcp-okn v0.1.0 (build 3638bbc)
- **Contents:** 1 query

## Knowledge graphs used

- `dreamkg` — <https://purl.org/okn/frink/kg/dreamkg>
- `medical-device-kg` — <https://purl.org/okn/frink/kg/medical-device-kg>

## Conversation

👤 **User**

Same crosswalk, coverage angle: how many DreamKG social-service ZIPs also carry a medical-device-kg address?

---

🧠 **Assistant**

**50 of DreamKG's 53 ZIPs — 94.3%.** Three postcodes have no device record at all.

The number is small in absolute terms and that is a property of DreamKG, not of the join: DreamKG is city-scoped (Philadelphia and San Francisco), so 53 ZIPs is essentially its entire footprint. What the crosswalk says is that **the FDA's device geography covers almost all of it** — in 50 of 53 neighbourhoods where DreamKG catalogues a social service, there is registered device equipment or a regulated establishment in the same postcode.

| Side | ZIPs | Shared |
|---|---|---|
| DreamKG service locations | 53 | 50 (94.3%) |
| medical-device-kg US addresses | 24,317 | 50 (0.2%) |

**How to use a 50-row crosswalk.** Not for statistics — for enumeration. With 50 ZIPs you can list every one and inspect it, which is exactly what makes the neighbourhood comparison in Q1 tractable: 19104 with 313 device records against 19140 with 85 is a difference you can look at directly rather than model.

**The three unmatched ZIPs** are the interesting residue: a DreamKG service catchment with no FDA-registered device presence at all. In a city as dense as Philadelphia that is worth a second look — most likely a PO-box or administrative ZIP rather than a real service desert.

## SPARQL queries executed

#### Query 1

_2026-09-02T05:17:28+00:00 · `dreamkg`, `medical-device-kg`_

```sparql
PREFIX mdo: <http://medicaldevice.com/ontology/>
PREFIX schema: <http://schema.org/>
SELECT (COUNT(DISTINCT ?zip) AS ?dreamkgZips) (COUNT(DISTINCT ?shared) AS ?alsoInMedicalDeviceKg) WHERE {
  { SELECT DISTINCT ?zip WHERE {
      GRAPH <https://purl.org/okn/frink/kg/dreamkg> { ?loc schema:postalCode ?z1 }
      BIND(REPLACE(STR(?z1),"[^0-9]","") AS ?e)
      BIND(IF(STRLEN(?e)=4, CONCAT("0",?e), SUBSTR(?e,1,5)) AS ?zip) } }
  OPTIONAL {
    { SELECT DISTINCT ?zip WHERE {
        GRAPH <https://purl.org/okn/frink/kg/medical-device-kg> {
          { ?s mdo:zip ?z0 } UNION { ?s mdo:postalCode ?z0 } }
        BIND(REPLACE(STR(?z0),"[^0-9]","") AS ?d)
        BIND(IF(STRLEN(?d)=4, CONCAT("0",?d), SUBSTR(?d,1,5)) AS ?zip)
        FILTER(REGEX(?zip,"^[0-9]{5}$")) } }
    BIND(?zip AS ?shared)
  }
}
```

_1 row(s)_

| dreamkgZips | alsoInMedicalDeviceKg |
| --- | --- |
| 53 | 50 |

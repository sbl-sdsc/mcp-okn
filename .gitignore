# Byte-compiled / optimized / cached
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/
wheels/

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*

# Editors / OS
.idea/
.vscode/
.DS_Store

# verify_skeletons.py live-run cache
scripts/.skeleton_results.json

# Dated metadata backups (live file is metadata/crosswalks.json; git history
# preserves prior versions)
metadata/*_20[0-9][0-9]-[0-1][0-9]-*.json

# Claude Code / agent local config — ignore everything under .claude/ EXCEPT the
# committed, team-wide project settings and hook scripts (settings.local.json etc. stay ignored).
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**

# Local scratch export
mcp.html
